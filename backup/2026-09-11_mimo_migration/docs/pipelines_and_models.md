# Пайплайны и модели — справочник

> Быстрая справка по всем пайплайнам и агентам системы.
> Язык: русский. Дата: июнь 2026.

---

## ЧАСТЬ 1: ПАЙПЛАЙНЫ

### Обозначения

```
[Агент]          — обычный шаг пайплайна
[Агент] ─(файл)─ — агент читает/пишет файл
[⟳ rework ×3]    — цикл реработки, максимум 3 итерации
──►               — направление потока
═══►              — переход на следующий этап
```

---

### 1. BUGFIX SIMPLE

**Когда используется:** Простые баги, не требующие глубокого планирования.

```
┌──────────────┐     ┌──────────┐     ┌─────────┐
│ bugfix-triage │ ──► │  worker  │ ──► │ utility │
└──────────────┘     └──────────┘     └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `bugfix-triage` | Первичный анализ бага: определяет критичность, область, воспроизводимость |
| `worker` | Реализует исправление — простые задачи, не требующие глубокого анализа |
| `utility` | Финальная валидация: синтаксис, форматирование, проверка стиля кода |

---

### 2. BUGFIX DEEP

**Когда используется:** Сложные баги, требующие планирования, ревью и циклов реработки.

```
┌──────────────┐     ┌──────────┐     ┌───────────────────┐     ┌──────────────┐
│ bugfix-triage │ ──► │ plan-bug │ ──► │   execute-bug     │ ──► │ dev-reviewer │
└──────────────┘     └──────────┘     └───────────────────┘     └──────────────┘
                     пишет                читает                       │
                   bug_plan.md          bug_plan.md                    ▼
                                                                 ┌──────────┐
                                                                 │ rework   │
                                                                 └──────────┘
                                                                       │
                                                                       ▼
                                                          ┌─────────────────────┐
                                                          │ consistency-checker  │
                                                          └─────────────────────┘
                                                                       │
                                                          ┌──── ── ── ─┴─ ── ── ────┐
                                                          │  [⟳ rework loop, max 3] │
                                                          └──── ── ── ─┬─ ── ── ────┘
                                                                       │
                                                                       ▼
                                                                 ┌─────────┐
                                                                 │ utility │
                                                                 └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `bugfix-triage` | Первичный анализ бага: воспроизведение, определение корневой причины |
| `plan-bug` | Составляет план исправления, записывает в `bug_plan.md` |
| `execute-bug` | Реализует исправление по плану из `bug_plan.md` |
| `dev-reviewer` | Ревью кода: проверяет качество, безопасность и корректность решения |
| `rework` | Исправляет замечания, выявленные на ревью |
| `consistency-checker` | Проверяет соответствие архитектуре проекта (читает `ARCHITECTURE.md`) |
| `utility` | Финальная валидация: синтаксис, форматирование |

> **⟳ Цикл реработки:** Если `consistency-checker` или `dev-reviewer` находят проблемы,
> управление возвращается к `rework` (до 3 итераций).

---

### 3. DEV SIMPLE (без плана)

**Когда используется:** Простые задачи разработки, когда план не требуется — прямая реализация.

```
┌──────────┐     ┌─────────┐
│  worker  │ ──► │ utility │
└──────────┘     └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `worker` | Реализует задачу напрямую, без предварительного планирования |
| `utility` | Финальная валидация: синтаксис, форматирование |

---

### 4. DEV SIMPLE (с планом)

**Когда используется:** Задачи средней сложности, когда план уже существует (`plan_exists=true`).

```
┌──────────┐     ┌─────────────────────┐     ┌─────────┐
│  worker  │ ──► │ consistency-checker │ ──► │ utility │
└──────────┘     └─────────────────────┘     └─────────┘
                         │
            ┌──── ── ── ─┴─ ── ── ────┐
            │  [⟳ rework loop, max 3] │
            └──── ── ── ─┬─ ── ── ────┘
                         │
                   (возврат к worker)
```

| Агент | Что делает |
|-------|-----------|
| `worker` | Реализует задачу в соответствии с планом |
| `consistency-checker` | Проверяет соответствие реализации плану и архитектуре |
| `utility` | Финальная валидация: синтаксис, форматирование |

> **⟳ Цикл реработки:** Если `consistency-checker` находит расхождения с планом,
> управление возвращается к `worker` (до 3 итераций).

---

### 5. DEV COMPLEX

**Когда используется:** Сложные задачи разработки, требующие планирования, наставничества и ревью.

```
┌──────────────┐     ┌───────────────┐     ┌──────────────┐     ┌──────────┐
│ dev-planner  │ ──► │ dev-professor │ ──► │ dev-reviewer │ ──► │  rework  │
└──────────────┘     └───────────────┘     └──────────────┘     └──────────┘
   пишет                читает
 dev_plan.md          dev_plan.md
                                                                       │
                                                                       ▼
                                                         ┌─────────────────────┐
                                                         │ consistency-checker │
                                                         └─────────────────────┘
                                                                      │
                                                         ┌──── ── ── ─┴─ ── ── ────┐
                                                         │  [⟳ rework loop, max 3] │
                                                         └──── ── ── ─┬─ ── ── ────┘
                                                                      │
                                                                      ▼
                                                                ┌─────────┐
                                                                │ utility │
                                                                └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `dev-planner` | Планирует архитектуру и подход к решению, **записывает план в `dev_plan.md`** |
| `dev-professor` | Реализует сложный код, следуя плану и лучшим практикам |
| `dev-reviewer` | Проводит ревью: качество, паттерны, потенциальные проблемы |
| `rework` | Исправляет замечания по ревью |
| `consistency-checker` | Валидирует соответствие архитектуре проекта |
| `utility` | Финальная валидация: синтаксис, форматирование |

> **⟳ Цикл реработки:** Если `consistency-checker` или `dev-reviewer` находят проблемы,
> управление возвращается к `rework` (до 3 итераций).

---

### 6. DEV SUPERCOMPLEX

**Когда используется:** Очень сложные задачи с планом >3 шагов или огромным объёмом работ.
Полный цикл ревью/валидации выполняется **для каждого шага** плана.

```
┌─────────────────────────────────────────────────────────────────┐
│  Для КАЖДОГО шага плана:                                        │
│                                                                 │
│  ┌──────────────┐   ┌───────────────┐   ┌──────────────┐       │
│  │ dev-planner  │──►│ dev-professor │──►│ dev-reviewer │       │
│  └──────────────┘   └───────────────┘   └──────────────┘       │
│    пишет               читает                                  │
│  dev_plan.md         dev_plan.md                               │
│                                              │                  │
│                                              ▼                  │
│                                 ┌─────────────────────┐         │
│                                 │ consistency-checker │         │
│                                 └─────────────────────┘         │
│                                              │                  │
│                                 ┌──── ── ── ─┴─ ── ── ────┐   │
│                                 │  [⟳ rework loop, max 3] │   │
│                                 └──── ── ── ─┬─ ── ── ────┘   │
│                                              │                  │
│                                              ▼                  │
│                                        ┌─────────┐             │
│                                        │ utility │             │
│                                        └─────────┘             │
└─────────────────────────────────────────────────────────────────┘
```

| Агент | Что делает |
|-------|-----------|
| `dev-planner` | Планирует конкретный шаг реализации, **записывает план в `dev_plan.md`** |
| `dev-professor` | Реализует код для текущего шага |
| `dev-reviewer` | Ревью кода текущего шага |
| `consistency-checker` | Проверяет архитектурное соответствие текущего шага |
| `utility` | Валидация синтаксиса после каждого шага |

> **⟳ Цикл реработки:** Для каждого шага — до 3 итераций реработки.
> Триггер: план с >3 шагами ИЛИ явный запрос пользователя ИЛИ огромный объём работ.

---

### 7. DEVOPS

**Когда используется:** Операции DevOps — деплой, инфраструктура, CI/CD.

```
┌───────────────┐     ┌─────────────────┐
│ devops-agent  │ ──► │ devops-reviewer │
└───────────────┘     └─────────────────┘
```

| Агент | Что делает |
|-------|-----------|
| `devops-agent` | Выполняет операции DevOps: деплой, настройка инфраструктуры, CI/CD |
| `devops-reviewer` | Проверяет конфигурации, скрипты и безопасность DevOps-операций |

---

### 8. DOCS SIMPLE

**Когда используется:** Простые задачи документации — обновление README, добавление комментариев.

```
┌─────────────┐     ┌─────────┐
│ docs-writer │ ──► │ utility │
└─────────────┘     └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `docs-writer` | Генерирует и обновляет документацию: README, комментарии, docstrings |
| `utility` | Финальная валидация: форматирование, проверка ссылок |

---

### 9. DOCS DEEP

**Когда используется:** Сложные задачи документации — архитектурные описания, API reference, гайды миграции.

```
┌──────────────┐     ┌─────────────┐     ┌──────────────┐     ┌──────────┐
│ docs-planner │ ──► │ docs-writer │ ──► │ dev-reviewer │ ──► │  rework  │
└──────────────┘     └─────────────┘     └──────────────┘     └──────────┘
  пишет                читает                                       │
docs_plan.md        docs_plan.md                                    ▼
                                                     ┌─────────────────────┐
                                                     │ consistency-checker │
                                                     └─────────────────────┘
                                                              │
                                                     ┌──── ── ── ─┴─ ── ── ────┐
                                                     │  [⟳ rework loop, max 3] │
                                                     └──── ── ── ─┬─ ── ── ────┘
                                                              │
                                                              ▼
                                                        ┌─────────┐
                                                        │ utility │
                                                        └─────────┘
```

| Агент | Что делает |
|-------|-----------|
| `docs-planner` | Анализирует код и планирует структуру документации, записывает `docs_plan.md` |
| `docs-writer` | Создаёт документацию по плану из `docs_plan.md` |
| `dev-reviewer` | Проверяет точность и полноту документации |
| `rework` | Исправляет замечания по ревью документации |
| `consistency-checker` | Проверяет соответствие документации реальному коду и архитектуре |
| `utility` | Финальная валидация форматирования и ссылок |

> **⟳ Цикл реработки:** Если `consistency-checker` или `dev-reviewer` находят проблемы,
> управление возвращается к `rework` (до 3 итераций).

---

### 10. Auto-DOCS Hook

**Когда используется:** Автоматически после завершения пайплайнов BUGFIX или DEV.

```
┌─────────────────────────────────────────┐
│  Завершён пайплайн BUGFIX / DEV        │
│  (последний шаг — utility)              │
└──────────────────┬──────────────────────┘
                   │
                   ▼
        ┌─────────────────────────┐
        │ requires_docs_update    │
        │        == true ?        │
        └────────┬───────┬────────┘
                 │       │
            Да   │       │  Нет
                 ▼       ▼
    ┌──────────────┐   ┌──────┐
    │ docs-writer  │   │ Конец│
    └──────┬───────┘   └──────┘
           │
           ▼
    ┌─────────┐
    │ utility │
    └─────────┘
```

**Условия срабатывания** (`requires_docs_update: true`):

- Был изменён `bug_plan.md` или `dev_plan.md`
- Были изменены любые `*.md` файлы (README, ARCHITECTURE, docs/)
- Изменилось публичное API
- Добавлены значимые docstrings

**Пайплайны с хуком:** BUGFIX SIMPLE, BUGFIX DEEP, DEV SIMPLE, DEV COMPLEX, DEV SUPERCOMPLEX.
**Пайплайны без хука:** DEVOPS, DOCS (рекурсивно), PLAN, RESEARCH.

---

### Сводная таблица пайплайнов

| Пайплайн | Шагов | Цикл реработки | Auto-DOCS |
|----------|-------|-----------------|-----------|
| BUGFIX SIMPLE | 3 | Нет | Да |
| BUGFIX DEEP | 7+ | Да (×3) | Да |
| DEV SIMPLE (без плана) | 2 | Нет | Да |
| DEV SIMPLE (с планом) | 3 | Да (×3) | Да |
| DEV COMPLEX | 7+ | Да (×3) | Да |
| DEV SUPERCOMPLEX | 7+ на шаг | Да (×3 на шаг) | Да |
| DEVOPS | 2 | Нет | Нет |
| DOCS SIMPLE | 2 | Нет | Нет |
| DOCS DEEP | 6+ | Да (×3) | Нет |

---

## ЧАСТЬ 2: МОДЕЛИ И ИХ НАЗНАЧЕНИЕ

### Общая архитектура

Система использует **двойную архитектуру первичных агентов**:
- **orchestrator** — операционные и исполнительные задачи (BUGFIX, DEVOPS, DEV, DOCS)
- **plankestrator** — плановые и исследовательские задачи (PLAN, RESEARCH)

Оба первичных агента работают на одной модели, но имеют разные routing tables и не вызывают друг друга.

---

### Первичные агенты

| Агент | Модель | Роль |
|-------|--------|------|
| `orchestrator` | Qwen 3.7 Plus | Управление операционными задачами: BUGFIX, DEVOPS, DEV, DOCS. Маршрутизация задач по агентам. |
| `plankestrator` | Qwen 3.7 Plus | Управление плановыми задачами: PLAN, RESEARCH. Координация исследований и планирования. |

**Почему Qwen 3.7 Plus:** Баланс производительности и качества для маршрутизации и координации. Модель хорошо справляется с пониманием контекста и принятием решений о том, какой агент должен обработать задачу.

---

### Агенты реализации и специалисты

#### dev-professor — GLM-5.2

| Параметр | Значение |
|----------|----------|
| **Модель** | GLM-5.2 |
| **Роль** | Сложная реализация кода |
| **Задачи** | Написание сложного кода, следование плану и лучшим практикам, архитектурные решения |
| **Почему эта модель** | Мощная модель для генерации качественного кода. Хорошо справляется с длинным контекстом и сложными логическими цепочками |
| **Ключевые возможности** | Генерация кода, рефакторинг, следование архитектурным паттернам |

#### dev-reviewer — Kimi K2.7 Code

| Параметр | Значение |
|----------|----------|
| **Модель** | Kimi K2.7 Code |
| **Роль** | Ревью кода |
| **Задачи** | Проверка качества кода, выявление потенциальных проблем, проверка безопасности и паттернов |
| **Почему эта модель** | Специализированная модель для анализа кода. Отлично находит баги, проблемы производительности и нарушения best practices |
| **Ключевые возможности** | Статический анализ, выявление code smells, проверка архитектуры |

#### rework — GLM-5.2

| Параметр | Значение |
|----------|----------|
| **Модель** | GLM-5.2 |
| **Роль** | Исправление замечаний по ревью |
| **Задачи** | Внесение исправлений в код по результатам ревью, устранение замечаний |
| **Почему эта модель** | Та же модель, что и у dev-professor — обеспечивает консистентность стиля кода при исправлениях |
| **Ключевые возможности** | Точечные исправления, рефакторинг, сохранение стиля кода |

#### execute-bug — GLM-5.2

| Параметр | Значение |
|----------|----------|
| **Модель** | GLM-5.2 |
| **Роль** | Реализация исправлений багов |
| **Задачи** | Исправление багов по плану из `bug_plan.md`, реализация патчей |
| **Почему эта модель** | Мощная модель для точного и качественного исправления кода |
| **Ключевые возможности** | Генерация исправлений, следование плану, работа с контекстом бага |

#### docs-writer — Xiaomi MiMo-V2.5-Pro

| Параметр | Значение |
|----------|----------|
| **Модель** | Xiaomi MiMo-V2.5-Pro |
| **Роль** | Написание документации |
| **Задачи** | Создание и обновление README, API reference, ARCHITECTURE, туториалов, docstrings |
| **Почему эта модель** | Хорошо генерирует структурированный текст, понимает контекст кода и создаёт понятную документацию |
| **Ключевые возможности** | Генерация markdown, docstrings, описание API, создание примеров |

#### bugfix — Qwen 3.7 Plus

| Параметр | Значение |
|----------|----------|
| **Модель** | Qwen 3.7 Plus |
| **Роль** | Простое исправление багов |
| **Задачи** | Исправление простых багов, не требующих глубокого анализа |
| **Почему эта модель** | Быстрая и надёжная для простых задач. Не нужна более мощная модель |
| **Ключевые возможности** | Быстрая генерация патчей, понимание контекста |

#### bugfix-triage — Qwen 3.7 Plus

| Параметр | Значение |
|----------|----------|
| **Модель** | Qwen 3.7 Plus |
| **Роль** | Первичный анализ багов |
| **Задачи** | Анализ баг-репорта, определение критичности, области поражения, воспроизводимости |
| **Почему эта модель** | Хорошее соотношение скорость/качество для аналитических задач |
| **Ключевые возможности** | Анализ логов, определение корневой причины, классификация |

#### plan-bug — MiniMax-M3

| Параметр | Значение |
|----------|----------|
| **Модель** | MiniMax-M3 |
| **Роль** | Планирование исправления багов |
| **Задачи** | Составление плана исправления, определение шагов, запись в `bug_plan.md` |
| **Почему эта модель** | Мультимодальная модель — эффективна для структурированного планирования и анализа контекста |
| **Ключевые возможности** | Структурированное планирование, декомпозиция задач |

#### devops-agent — MiniMax-M3

| Параметр | Значение |
|----------|----------|
| **Модель** | MiniMax-M3 |
| **Роль** | DevOps-операции |
| **Задачи** | Деплой, настройка инфраструктуры, CI/CD, управление серверами |
| **Почему эта модель** | Мультимодальная модель с поддержкой видео — эффективна для анализа логов и конфигураций |
| **Ключевые возможности** | Работа с конфигурациями, скриптами автоматизации, контейнерами |

#### devops-reviewer — Qwen 3.7 Plus

| Параметр | Значение |
|----------|----------|
| **Модель** | Qwen 3.7 Plus |
| **Роль** | Ревью DevOps-конфигураций |
| **Задачи** | Проверка конфигураций, скриптов, безопасности DevOps-операций |
| **Почему эта модель** | Достаточно для анализа конфигурационных файлов и скриптов |
| **Ключевые возможности** | Анализ Docker, CI/CD, security audit |

#### dev-planner — Qwen 3.7 Plus

| Параметр | Значение |
|----------|----------|
| **Модель** | Qwen 3.7 Plus |
| **Роль** | Планирование разработки |
| **Задачи** | Архитектурное планирование, декомпозиция сложных задач, **запись плана в `dev_plan.md`** |
| **Почему эта модель** | Баланс производительности и качества для архитектурного планирования |
| **Ключевые возможности** | Структурированное планирование, определение подходов, **обязательная запись плана в файл `dev_plan.md`** для чтения dev-professor |

#### consistency-checker — Qwen 3.7 Plus

| Параметр | Значение |
|----------|----------|
| **Модель** | Qwen 3.7 Plus |
| **Роль** | Проверка архитектурной консистентности |
| **Задачи** | Валидация соответствия кода архитектуре проекта (читает `ARCHITECTURE.md`), проверка routing tables, pipeline definitions, JSON validation |
| **Почему эта модель** | Надёжная для анализа соответствия и структурированных проверок |
| **Ключевые возможности** | Сравнение с эталоном, проверка конфигураций, валидация структуры |

#### worker — MiniMax-M3

| Параметр | Значение |
|----------|----------|
| **Модель** | MiniMax-M3 |
| **Роль** | Простые задачи разработки |
| **Задачи** | Реализация простых задач, прямое написание кода |
| **Почему эта модель** | Мультимодальная модель с поддержкой видео — эффективна для анализа контекста и генерации кода |
| **Ключевые возможности** | Быстрая реализация, работа с файлами, запуск команд |

#### utility — MiniMax-M2.7

| Параметр | Значение |
|----------|----------|
| **Модель** | MiniMax-M2.7 |
| **Роль** | Финальная валидация |
| **Задачи** | Проверка синтаксиса, форматирование, linting, проверка стиля кода |
| **Почему эта модель** | Быстрая и экономичная для рутинных проверок. Не нужна мощная модель для форматирования |
| **Ключевые возможности** | ESLint, Prettier, проверка форматирования, базовая валидация |

#### summarizer — MiniMax-M2.7

| Параметр | Значение |
|----------|----------|
| **Модель** | MiniMax-M2.7 |
| **Роль** | Суммаризация контента |
| **Задачи** | Краткое изложение длинных текстов, отчётов, логов |
| **Почему эта модель** | Быстрая и экономичная для задач суммаризации |
| **Ключевые возможности** | Извлечение ключевых фактов, создание резюме |

#### view-image — Kimi K2.6

| Параметр | Значение |
|----------|----------|
| **Модель** | bifrost-litellm/Kimi K2.6 |
| **Роль** | Анализ изображений |
| **Задачи** | Анализ скриншотов, диаграмм, UI-мокапов, извлечение текста из изображений |
| **Почему эта модель** | Прямые vision-возможности — модель может «видеть» и анализировать изображения |
| **Ключевые возможности** | Vision (зрение), описание изображений, OCR, анализ UI |

---

### Сводная таблица моделей

| Модель | Агент(ы) | Тип задач |
|--------|----------|-----------|
| **bifrost-litellm/QWEN3.7-plus** | orchestrator, plankestrator, bugfix, bugfix-triage, dev-planner, docs-planner, devops-reviewer, consistency-checker, plan-writer-simple, orchestrator-identity-probe, plankestrator-identity-probe | Координация, анализ, планирование |
| **bifrost-litellm/MiniMax-M3** | worker, plan-bug, devops-agent, devops-readonly | Реализация, triage, планирование багов, DevOps |
| **bifrost-litellm/GLM-5.2** | dev-professor, rework, execute-bug, plan-writer-complex, research-writer-complex | Сложная реализация кода, исправления |
| **bifrost-litellm/Kimi K2.7** | dev-reviewer, plan-reviewer-simple, plan-reviewer-complex, research-reviewer | Ревью кода, планов, исследований |
| **bifrost-litellm/MiniMax-M2.7** | mcp-github, mcp-read, mcp-search, summarizer, utility | MCP инструменты, суммаризация, валидация |
| **bifrost-litellm/mimo-v2.5-pro** | docs-writer, research-writer-simple | Документация, исследование |
| **bifrost-litellm/Kimi K2.6** | view-image | Анализ изображений (vision) |

---

### Иерархия агентов по сложности

```
 Простые задачи ──────────────────────────────────────────── Сложные задачи
      │                                                           │
      ▼                                                           ▼
   utility          worker / bugfix        dev-professor      orchestrator
 (MiniMax-M2.7)    (MiniMax-M3 / Qwen 3.7) (GLM-5.2)         (Qwen 3.7 Plus)
      │                │                      │                    │
  Форматирование   Реализация           Сложный код          Координация
  синтаксис        простых задач        архитектура          маршрутизация
```

---

> **Примечание:** Модели, отмеченные как *«не указана»*, могут использовать модель по умолчанию
> или быть настроены в конфигурации `~/.config/opencode/opencode.json`.
